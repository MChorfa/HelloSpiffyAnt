<ivysettings>
  <settings defaultResolver="main"/>
  <resolvers>
    <chain name="main">
      <!-- This server resolves most our external dependencies. -->
      <ibiblio name="sonatype"
                 m2compatible="true"
                 usepoms="true"
                 pattern="[organisation]/[module]/[revision]/[artifact]-[revision].[ext]"
                 root="https://oss.sonatype.org/service/local/repositories/central/content"/>
      
      <!-- This server resolves our internal dependencies. -->
      <ibiblio name="novell-snapshots"
                 m2compatible="true"
                 usepoms="true"
                 pattern="[organisation]/[module]/[revision]/[artifact]-[revision](-[classifier]).[ext]"
                 root="http://151.155.230.64:8081/nexus/service/local/repositories/snapshots/content"/>
                 
      <ibiblio name="default" m2compatible="true"/>
    </chain>
  </resolvers>
</ivysettings>
